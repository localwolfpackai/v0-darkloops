# Design snapshots

Reference screenshots of the DarkLoops component lab — kept here (not in the repo root) so the
root stays clean and these read as what they are: a visual record of the design.

| File | What it shows |
|------|---------------|
| `hero-now.png` | Hero section — earlier state (the "before") |
| `hero-after.png` | Hero section — reworked state (the "after") |
| `cards.png` | Card component variants |
| `effects.png` | Motion / effect showcase |
| `footer.png` | Footer layout |

`hero-now.png` → `hero-after.png` is the redesign before/after — the pair to reach for when this
project becomes a case study or social post.

> These are static reference images, not used at runtime by the app.
